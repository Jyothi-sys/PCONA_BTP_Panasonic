PROCEDURE "GET_EDIFACTHEADER_ID"(OUT NEXT_ID "NEXT_ID"  )
    LANGUAGE SQLSCRIPT
    SQL SECURITY INVOKER
    --DEFAULT SCHEMA <default_schema_name>
    READS SQL DATA AS
 BEGIN
    
    NEXT_ID = SELECT  "Edifact_Header_ID".NEXTVAL AS ID  FROM DUMMY;
 END