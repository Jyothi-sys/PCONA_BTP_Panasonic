PROCEDURE "GET_SEQUENCE_NO"(OUT NEXT_ID "NEXT_ID"  )
    LANGUAGE SQLSCRIPT
    SQL SECURITY INVOKER
    --DEFAULT SCHEMA <default_schema_name>
    READS SQL DATA AS
 BEGIN
    
    NEXT_ID = SELECT  "PO_List_SEQUENCE_NO".NEXTVAL AS ID  FROM DUMMY;
 END